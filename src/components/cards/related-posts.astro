---
interface RelatedPost {
	title: string;
	description: string;
	slug: string;
	sectionLabel: string;
}

interface Props {
	posts: RelatedPost[];
	heading?: string;
}

const { posts, heading = "keep reading" } = Astro.props;
---

{
	posts.length > 0 && (
		<nav
			aria-label='related posts'
			class='w-full max-w-xl flex flex-col gap-4 pt-10 border-t border-[var(--color-border)] not-prose'>
			<p class='text-sm font-medium uppercase tracking-wide text-[var(--color-text-secondary)]'>{heading}</p>
			<ul class='flex flex-col gap-4'>
				{posts.map((post) => (
					<li>
						<a
							href={`/${post.slug}`}
							class='group flex flex-col gap-1 no-underline'>
							<span class='text-xs font-medium uppercase tracking-wide text-[var(--color-text-secondary)]'>
								{post.sectionLabel}
							</span>
							<span class='type-body text-[var(--color-text)] group-hover:underline underline-offset-2'>
								{post.title}
							</span>
							<span class='text-sm font-medium text-[var(--color-text-secondary)] line-clamp-2'>
								{post.description}
							</span>
						</a>
					</li>
				))}
			</ul>
		</nav>
	)
}
